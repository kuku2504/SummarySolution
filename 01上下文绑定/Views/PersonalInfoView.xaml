<UserControl x:Class="_01上下文绑定.Views.PersonalInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:_01上下文绑定.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <Label Foreground="Red" FontWeight="Bold">学校：</Label>
            <TextBlock Text="{Binding UnitName}" Foreground="Blue" FontWeight="Bold"></TextBlock>
            <Label Foreground="Red" FontWeight="Bold">成绩：</Label>
            <TextBlock Text="{Binding CloudCoinNum}" Foreground="Blue" FontWeight="Bold"></TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
